import React, { Fragment } from 'react';
import Repositories from '../me/Repositories';

const Projects = () => {
	return (
		<Fragment>
			<Repositories />
		</Fragment>
	);
};

export default Projects;
